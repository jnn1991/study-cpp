#pragma once

#include <iostream>
#include <string>
#include <map>
#include <vector>

enum Color{
    RED,
    BLUE,
    NOTSET
};

enum WarriorType
{
    DRAGON_TYPE,
    NINJA_TYPE,
    ICEMAN_TYPE,
    LION_TYPE,
    WOLF_TYPE
};

class func {
public:
    func() {}
    ~func() {}

private:

};

